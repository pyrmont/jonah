<!DOCTYPE html>
<html>
<head>
  <title>List</title>
</head>
<body>
  <ol>
    <% for entry in @content['entries'] %>
      <li><a href="<%= entry.uri %>"><%= entry.name %></a></li>
    <% end %>
  </ol>
</body>
</html>